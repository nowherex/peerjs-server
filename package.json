{
  "name": "peerjs-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "set PORT=9000 && npm run start-windows",
    "start": "npx peerjs --port $PORT --key peerjs --path /",
    "start-windows": "npx peerjs --port %PORT% --key peerjs --path /",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "16"
  },
  "dependencies": {
    "peer": "^0.6.1"
  }
}
